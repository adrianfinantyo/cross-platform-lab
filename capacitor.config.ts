import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.adrianfinantyo.ionic.memories",
  appName: "Memories",
  webDir: "build",
  bundledWebRuntime: false,
};

export default config;
